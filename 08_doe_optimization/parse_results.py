from __future__ import annotations

# TODO:
# - parse simulator outputs into one row per design/device
# - retain bias conditions and units in column names
# - write all_metrics.csv
# - report missing and failed metrics instead of silently dropping them
